

import 'package:flutter/material.dart';

class ScreenSearchJob extends StatelessWidget {
  const ScreenSearchJob({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}